# crop-yield-prediction
Crop Yield Prediction is a machine learning web app built with Streamlit and scikit-learn. It uses a Decision Tree Regressor trained on historical Indian crop data (crop, season, state, rainfall, fertilizer, pesticide, area) to predict expected yield. Includes built-in sample test cases for quick verification.
